ls                                       //to show files
ls -force                                //.git show
git init                                  //initialize .git
git status                                //to show the status
git add "file name"                       //addding the file,untracked to the tracked
git add .                                 // adding all files
git commit -m "added testing files"       // tracking the file
rm filename                               //deleting the file
git restore filename                      //restoring the file
clear                                     //to clear the terminal

//steps for github repo-
git init
git add "file name"
git status
git add .
git commit -m "Added new file"
git remote add origin https://github.com/Prathamesh-Patil1/practice-sheet.git
git branch -m main
git push origin main

//steps fro updating file-
git add .
git commit -m "Update file"
git push origin master